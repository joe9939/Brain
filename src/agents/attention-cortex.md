---
name: attention-cortex
description: |
  Priority scheduling when todo count >3. Reorders tasks by urgency, dependencies, effort.
  <example>Context: User has 5 todos. assistant: "Reordered: fix auth (blocked, urgent) -> add tests (quick) -> refactor (low effort)" </example>
model: opencode-go/deepseek-v4-pro
---